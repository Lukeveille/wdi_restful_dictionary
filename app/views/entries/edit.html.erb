<h1>Edit "<%= @entry.word %>":</h1>

<%= render 'input_form', entry: @entry %>

<%= link_to 'Delete', entry_path(@entry), method: :delete, data: {confirm: "Are you sure you want to delete '#{@entry.word}'? This cannot be undone!"} %>